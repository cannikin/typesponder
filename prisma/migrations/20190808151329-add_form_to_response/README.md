# Migration `20190808151329-add_form_to_response`

This migration has been generated by Rob Cameron at 8/8/2019, 3:13:29 PM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "lift"."Response" ADD COLUMN "form" INTEGER   REFERENCES "Form"(id);
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration 20190808150249-add_submitted_at_to_response..20190808151329-add_form_to_response
--- datamodel.dml
+++ datamodel.dml
@@ -13,8 +13,9 @@
   uid       String  @unique
   name      String
   tag       String
   questions Question[]
+  responses Response[]
 }
 model Question {
   id        Int      @id @unique
@@ -40,8 +41,9 @@
   id          Int      @id @unique
   uid         String   @unique
   submittedAt DateTime
   user        User?
+  form        Form?
   answers     Answer[]
   createdAt   DateTime @default(now())
   updatedAt   DateTime @updatedAt
 }
```

## Photon Usage

You can use a specific Photon built for this migration (20190808151329-add_form_to_response)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20190808151329-add_form_to_response'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
