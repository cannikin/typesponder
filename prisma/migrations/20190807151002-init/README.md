# Migration `20190807151002-init`

This migration has been generated by Rob Cameron at 8/7/2019, 3:10:02 PM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "file:dev"."Form"("id" INTEGER NOT NULL DEFAULT 0 ,"uid" TEXT NOT NULL DEFAULT '' ,"name" TEXT NOT NULL DEFAULT '' ,"tag" TEXT NOT NULL DEFAULT '' ,PRIMARY KEY ("id"));

CREATE TABLE "file:dev"."Question"("id" INTEGER NOT NULL DEFAULT 0 ,"uid" TEXT NOT NULL DEFAULT '' ,"type" TEXT NOT NULL DEFAULT '' ,"text" TEXT NOT NULL DEFAULT '' ,"createdAt" DATE NOT NULL  ,"updatedAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00' ,"form" INTEGER   REFERENCES "Form"(id),PRIMARY KEY ("id"));

CREATE TABLE "file:dev"."Answer"("id" INTEGER NOT NULL DEFAULT 0 ,"uid" TEXT NOT NULL DEFAULT '' ,"type" TEXT NOT NULL DEFAULT '' ,"text" TEXT NOT NULL DEFAULT '' ,"createdAt" DATE NOT NULL  ,"updatedAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00' ,"question" INTEGER   REFERENCES "Question"(id),"response" INTEGER   REFERENCES "Response"(id),PRIMARY KEY ("id"));

CREATE TABLE "file:dev"."Response"("id" INTEGER NOT NULL DEFAULT 0 ,"uid" TEXT NOT NULL DEFAULT '' ,"createdAt" DATE NOT NULL  ,"updatedAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00' ,"user" INTEGER   REFERENCES "User"(id),PRIMARY KEY ("id"));

CREATE TABLE "file:dev"."User"("id" INTEGER NOT NULL DEFAULT 0 ,"email" TEXT NOT NULL DEFAULT '' ,"createdAt" DATE NOT NULL  ,"updatedAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00' ,PRIMARY KEY ("id"));

CREATE UNIQUE INDEX "file:dev"."Form.id._UNIQUE" ON "Form"("id")

CREATE UNIQUE INDEX "file:dev"."Form.uid._UNIQUE" ON "Form"("uid")

CREATE UNIQUE INDEX "file:dev"."Question.id._UNIQUE" ON "Question"("id")

CREATE UNIQUE INDEX "file:dev"."Question.uid._UNIQUE" ON "Question"("uid")

CREATE UNIQUE INDEX "file:dev"."Answer.id._UNIQUE" ON "Answer"("id")

CREATE UNIQUE INDEX "file:dev"."Answer.uid._UNIQUE" ON "Answer"("uid")

CREATE UNIQUE INDEX "file:dev"."Response.id._UNIQUE" ON "Response"("id")

CREATE UNIQUE INDEX "file:dev"."Response.uid._UNIQUE" ON "Response"("uid")

CREATE UNIQUE INDEX "file:dev"."User.id._UNIQUE" ON "User"("id")

CREATE UNIQUE INDEX "file:dev"."User.email._UNIQUE" ON "User"("email")
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration ..20190807151002-init
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,56 @@
+datasource db {
+  provider = "sqlite"
+  url      = "file:dev.db"
+  default  = true
+}
+
+generator photon {
+  provider = "photonjs"
+}
+
+model Form {
+  id        Int     @id @unique
+  uid       String  @unique
+  name      String
+  tag       String
+  questions Question[]
+}
+
+model Question {
+  id        Int      @id @unique
+  uid       String   @unique
+  type      String
+  text      String
+  form      Form?
+  answers   Answer[]
+  createdAt DateTime @default(now())
+  updatedAt DateTime @updatedAt
+}
+
+model Answer {
+  id        Int      @id @unique
+  uid       String   @unique
+  type      String
+  text      String
+  question  Question?
+  response  Response?
+  createdAt DateTime @default(now())
+  updatedAt DateTime @updatedAt
+}
+
+model Response {
+  id        Int      @id @unique
+  uid       String   @unique
+  user      User?
+  answers   Answer[]
+  createdAt DateTime @default(now())
+  updatedAt DateTime @updatedAt
+}
+
+model User {
+  id        Int      @id @unique
+  email     String   @unique
+  responses Response[]
+  createdAt DateTime @default(now())
+  updatedAt DateTime @updatedAt
+}
```

## Photon Usage

You can use a specific Photon built for this migration (20190807151002-init)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20190807151002-init'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
