# Migration `20190808122210-remove_answers_type`

This migration has been generated by Rob Cameron at 8/8/2019, 12:22:10 PM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
PRAGMA foreign_keys=OFF;

CREATE TABLE "lift"."new_Answer"("id" INTEGER NOT NULL DEFAULT 0 ,"text" TEXT NOT NULL DEFAULT '' ,"createdAt" DATE NOT NULL  ,"updatedAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00' ,"question" INTEGER   REFERENCES "Question"(id),"response" INTEGER   REFERENCES "Response"(id),PRIMARY KEY ("id"));

INSERT INTO "new_Answer" ("id","text","createdAt","updatedAt","question","response") SELECT "id","text","createdAt","updatedAt","question","response" from "Answer"

DROP TABLE "lift"."Answer";

ALTER TABLE "lift"."new_Answer" RENAME TO "Answer";

PRAGMA "lift".foreign_key_check;

PRAGMA foreign_keys=ON;
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration 20190808121818-remove_answer_uid..20190808122210-remove_answers_type
--- datamodel.dml
+++ datamodel.dml
@@ -28,9 +28,8 @@
 }
 model Answer {
   id        Int      @id @unique
-  type      String
   text      String
   question  Question?
   response  Response?
   createdAt DateTime @default(now())
```

## Photon Usage

You can use a specific Photon built for this migration (20190808122210-remove_answers_type)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20190808122210-remove_answers_type'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
