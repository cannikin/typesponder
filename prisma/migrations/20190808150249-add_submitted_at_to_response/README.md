# Migration `20190808150249-add_submitted_at_to_response`

This migration has been generated by Rob Cameron at 8/8/2019, 3:02:49 PM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
PRAGMA foreign_keys=OFF;

CREATE TABLE "lift"."new_Response"("id" INTEGER NOT NULL DEFAULT 0 ,"uid" TEXT NOT NULL DEFAULT '' ,"submittedAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00' ,"createdAt" DATE NOT NULL  ,"updatedAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00' ,"user" INTEGER   REFERENCES "User"(id),PRIMARY KEY ("id"));

INSERT INTO "new_Response" ("id","uid","createdAt","updatedAt","user") SELECT "id","uid","createdAt","updatedAt","user" from "Response"

DROP TABLE "lift"."Response";

ALTER TABLE "lift"."new_Response" RENAME TO "Response";

PRAGMA "lift".foreign_key_check;

PRAGMA foreign_keys=ON;
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration 20190808122210-remove_answers_type..20190808150249-add_submitted_at_to_response
--- datamodel.dml
+++ datamodel.dml
@@ -36,14 +36,15 @@
   updatedAt DateTime @updatedAt
 }
 model Response {
-  id        Int      @id @unique
-  uid       String   @unique
-  user      User?
-  answers   Answer[]
-  createdAt DateTime @default(now())
-  updatedAt DateTime @updatedAt
+  id          Int      @id @unique
+  uid         String   @unique
+  submittedAt DateTime
+  user        User?
+  answers     Answer[]
+  createdAt   DateTime @default(now())
+  updatedAt   DateTime @updatedAt
 }
 model User {
   id        Int      @id @unique
```

## Photon Usage

You can use a specific Photon built for this migration (20190808150249-add_submitted_at_to_response)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20190808150249-add_submitted_at_to_response'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
