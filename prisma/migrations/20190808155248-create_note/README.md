# Migration `20190808155248-create_note`

This migration has been generated by Rob Cameron at 8/8/2019, 3:52:48 PM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "lift"."Note"("id" INTEGER NOT NULL DEFAULT 0 ,"text" TEXT NOT NULL DEFAULT '' ,"user" INTEGER NOT NULL  REFERENCES "User"(id),PRIMARY KEY ("id"));

CREATE UNIQUE INDEX "lift"."Note.id._UNIQUE" ON "Note"("id")
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration 20190808151329-add_form_to_response..20190808155248-create_note
--- datamodel.dml
+++ datamodel.dml
@@ -54,4 +54,10 @@
   responses Response[]
   createdAt DateTime @default(now())
   updatedAt DateTime @updatedAt
 }
+
+model Note {
+  id   Int @id @unique
+  text String
+  user User
+}
```

## Photon Usage

You can use a specific Photon built for this migration (20190808155248-create_note)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20190808155248-create_note'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
